spring:
  application:
    name: api

  datasource:
    url: jdbc:postgresql://localhost:5432/eatzdev
    driver-class-name: org.postgresql.Driver
    username:
    password:

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
